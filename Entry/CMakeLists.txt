set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(${DRIVER_PROJECT_NAME} src/main.cpp)
set_target_properties(${DRIVER_PROJECT_NAME} PROPERTIES OUTPUT_NAME "${EXE_FILE_NAME}")
target_link_libraries(${DRIVER_PROJECT_NAME} PRIVATE "${LIBRARY_PROJECT_NAME}")